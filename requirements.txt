# Phase 8 of the migration replaces this file with pyproject.toml + a uv lockfile.
# Until then these are the direct dependencies, with the minimums actually tested.
#
# Python 3.12 is required: TensorFlow has no 3.13+ wheels, and 3.12 is the
# version every library on the migration roadmap supports.

# --- API ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.9
pydantic-settings>=2.4

# --- jobs ---
rq>=2.0
redis>=5.0

# --- numerical core ---
numpy>=2.0
pandas>=2.2
scipy>=1.14
scikit-learn>=1.5

# --- market data ---
yfinance>=0.2.40

# --- forecasting ---
# Install torch from the CPU index to avoid pulling ~2.5 GB of CUDA wheels:
#   uv pip install --index-url https://download.pytorch.org/whl/cpu torch
torch>=2.4
lightgbm>=4.3

# --- forecasting, legacy ---
# Only needed for the `keras_lstm` backend, which exists so the original result
# can be reproduced. Drop this line and the backend disappears from /health
# cleanly; nothing else depends on it.
tensorflow-cpu>=2.17

# --- interim UI (Phase 9 replaces this with React + TypeScript) ---
streamlit>=1.38

# --- development ---
pytest>=8.0
httpx>=0.27
# In-process Redis fake: covers the RQ protocol without a server.
fakeredis>=2.20
# Bundles a real Redis server, so the multi-process worker tests can run without
# Docker or root. Optional -- tests/test_worker_process.py skips without it.
redislite>=6.0
